#include "RainFallStatistics.h"
#include "RainFallMonth.h"
#include <iostream>
#include <string>

using namespace std;

int main()
{
	//implement main
	/*
	RainFallStatistics<RainFallMonth> rainFallStatistics;
	RainFallMonth rainFallMonth;
	string monthName;
	float rainfall;
	int monthNumber;
	int choice;
	do
	{
		cout << "1. Add a month" << endl;
		cout << "2. Display a month" << endl;
		cout << "3. Display the year" << endl;
		cout << "4. Display the average rainfall" << endl;
		cout << "5. Display the total rainfall" << endl;
		cout << "6. Display the month with the most rainfall" << endl;
		cout << "7. Display the month with the least rainfall" << endl;
		cout << "8. Exit" << endl;
		cout << "Enter your choice: ";
		cin >> choice;
		switch (choice)
		{
		case 1:
			cout << "Enter the month name: ";
			cin >> monthName;
			cout << "Enter the rainfall: ";
			cin >> rainfall;
			rainFallMonth = RainFallMonth(monthName, rainfall);
			rainFallStatistics.addRainFallMonth(rainFallMonth);
			break;
		case 2:
			cout << "Enter the month name: ";
			cin >> monthName;
			rainFallStatistics.displayRainFallMonth(monthName);
			break;
		case 3:
			rainFallStatistics.displayRainFallYear();
			break;
		case 4:
			rainFallStatistics.displayRainFallMonthAverage();
			break;
		case 5:
			rainFallStatistics.displayRainFallMonthTotal();
			break;
		case 6:
			rainFallStatistics.displayRainFallMonthMax();
			break;
		case 7:
			rainFallStatistics.displayRainFallMonthMin();
			break;
		case 8:
			cout << "Goodbye!" << endl;
			break;
		default:
			cout << "Invalid choice" << endl;
		}
	} while (choice != 8);
	*/
}